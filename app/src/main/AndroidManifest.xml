<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.habithelper">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GoodSpecificHabit" />
        <activity android:name=".BadSpecificHabit" />
        <activity
            android:name=".Profile_Page"
            android:label="Profile Page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Friend_Page"
            android:label="Friend Page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Trophies"
            android:label="@string/title_activity_trophies"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".habitDetail"
            android:label="Specific Habit"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".home"
            android:label="Home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".friends_feed"
            android:label="Friends Feed"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CreateNewUserActivity"
            android:label="@string/title_activity_create_new_user"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>